<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="matchmaking.Views.NotificationsView"
    x:Name="NotificationsViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="40,20,40,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="NOTIFICATIONS" FontSize="36" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Mark all as read" Click="HandleMarkAllAsReadClick"
                    Background="Transparent" BorderBrush="#FF5A5F" BorderThickness="2" Foreground="#FF5A5F"/>
            <Button Grid.Column="2" Content="Delete all" Click="HandleDeleteAllClick"
                    Background="Transparent" BorderBrush="#FF5A5F" BorderThickness="2" Foreground="#FF5A5F"/>
        </Grid>

        <ListView Grid.Row="2" x:Name="NotificationsList" SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>